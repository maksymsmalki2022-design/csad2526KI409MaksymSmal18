The script ci.sh should:
1. Create a build directory (mkdir build)
2. Move into it (cd build)
3. Configure the project with CMake (cmake ..)
4. Build the project (cmake --build .)
5. Run tests with CTest (ctest)
6. On Linux/macOS, ensure the script is executable (chmod +x ci.sh)
7. Add Git commands to commit the script (git add ci.sh && git commit -m
"Add Cl build script")
Make sure the commands are cross-platform and can run on Linux, macOS, and Windows.
For Windows, generate ci.bat that performs equivalent steps.

› Implementing Sequential Actions...
I'll add Cl script files (s ci.sh and ci.bat), then make s ci.sh executable and commit both files. Expected outcome: new scripts added, ci.sh made executable, and a git commit created. I'll update the todo list before and after these steps.
v Added 3 todos
Now I'll create/update the POSIX shell script $ ci.sh
and the
Windows batch ci.bat with cross-platform sensible steps (configure, build, test). Then I'll make $ ci.sh executable and
commit both files.
I'll first add the two files.
$ ci.sh +40 -1
I'll reapply the batch file patch using REM comments (avoid'::' which caused a patch parse error).